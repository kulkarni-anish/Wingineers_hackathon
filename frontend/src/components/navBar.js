import React from "react";
import "../styles/navBar.scss";
import Typography from "@mui/material/Typography";
const NavBar = () => {
  return (
    <div className="navBar">
      <Typography className="navBar-header">alibaba</Typography>
    </div>
  );
};

export default NavBar;
